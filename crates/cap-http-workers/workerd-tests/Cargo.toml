[package]
name = "cap-http-workers-test"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
publish = false

# Standalone workspace - not part of parent workspace
[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cap-http-workers = { path = ".." }
capabilities = { path = "../../capabilities" }
worker = { version = "0.7.4", features = ["http"] }
futures-util = "0.3"
